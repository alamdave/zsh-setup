# ~/.zsh-configs/theme.zsh

# Powerlevel10k theme
source ~/.zsh-configs/powerlevel10k/powerlevel10k.zsh-theme

# Load Powerlevel10k configuration if it exists
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh